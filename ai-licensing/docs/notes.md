## Important Facts

👉 You cannot reliably detect AI bots from frontend JavaScript.
Because:

Browsers don’t expose the true User-Agent of bots.

AI crawlers don’t execute your client-side JS.

Serious AI scrapers fetch your site server-side.

So this detection must happen on the server.
